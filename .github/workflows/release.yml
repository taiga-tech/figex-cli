name: Release

on:
  workflow_call:

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-latest
            platform_pkg: cli-darwin-arm64
          - target: x86_64-apple-darwin
            os: macos-latest
            platform_pkg: cli-darwin-x64
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            platform_pkg: cli-win32-x64
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            platform_pkg: cli-linux-x64-gnu
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            platform_pkg: cli-linux-x64-musl

    steps:
      - uses: actions/checkout@v6

      - uses: jdx/mise-action@v4

      - name: Add Rust target
        run: rustup target add ${{ matrix.target }}

      - name: Install musl tools (musl target only)
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Build binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Copy binary to platform package (Unix)
        if: runner.os != 'Windows'
        run: |
          cp target/${{ matrix.target }}/release/figex-cli \
            packages/${{ matrix.platform_pkg }}/vendor/${{ matrix.target }}/figex-cli/figex-cli

      - name: Copy binary to platform package (Windows)
        if: runner.os == 'Windows'
        run: |
          copy target\${{ matrix.target }}\release\figex-cli.exe `
            packages\${{ matrix.platform_pkg }}\vendor\${{ matrix.target }}\figex-cli\figex-cli.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: binary-${{ matrix.target }}
          path: packages/${{ matrix.platform_pkg }}/vendor/${{ matrix.target }}/figex-cli/

  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: [build]
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v6

      - uses: jdx/mise-action@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: binary-*
          merge-multiple: false

      - name: Place binaries into vendor directories
        run: |
          cp -r binary-aarch64-apple-darwin/. packages/cli-darwin-arm64/vendor/aarch64-apple-darwin/figex-cli/
          cp -r binary-x86_64-apple-darwin/. packages/cli-darwin-x64/vendor/x86_64-apple-darwin/figex-cli/
          cp -r binary-x86_64-pc-windows-msvc/. packages/cli-win32-x64/vendor/x86_64-pc-windows-msvc/figex-cli/
          cp -r binary-x86_64-unknown-linux-gnu/. packages/cli-linux-x64-gnu/vendor/x86_64-unknown-linux-gnu/figex-cli/
          cp -r binary-x86_64-unknown-linux-musl/. packages/cli-linux-x64-musl/vendor/x86_64-unknown-linux-musl/figex-cli/

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build launcher
        run: pnpm --filter @taiga-tech/figex-cli build

      - name: Configure npm auth
        run: |
          printf "//registry.npmjs.org/:_authToken=%s\n" "$NODE_AUTH_TOKEN" > ~/.npmrc
          printf "registry=https://registry.npmjs.org/\n" >> ~/.npmrc
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      # Publish platform packages first, then the launcher (order is required)
      - name: Publish platform packages
        run: |
          pnpm --filter @taiga-tech/figex-cli-darwin-arm64 publish --no-git-checks
          pnpm --filter @taiga-tech/figex-cli-darwin-x64 publish --no-git-checks
          pnpm --filter @taiga-tech/figex-cli-win32-x64 publish --no-git-checks
          pnpm --filter @taiga-tech/figex-cli-linux-x64-gnu publish --no-git-checks
          pnpm --filter @taiga-tech/figex-cli-linux-x64-musl publish --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish launcher
        run: pnpm --filter @taiga-tech/figex-cli publish --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
